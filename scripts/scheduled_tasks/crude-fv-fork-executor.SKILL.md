---
name: crude-fv-fork-executor
description: Daily 12:45: execute every open fork in inputs/forks.yaml whose silence window has passed (the 2026-09-10 policy: silence executes the recommendation). Bounded authority: execute exactly as registered, regen, run the suite and the gate, check the predicted band, annotate, commit; halt and page the owner on any miss.
allowed-tools: Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.forks:*), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.rebase:*), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.drift_gate), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.reconcile:*), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.notify --send page:*), Bash(PYTHONPATH=src .venv/bin/python -m pytest:*), Bash(bash scripts/regen.sh:*), Bash(git status:*), Bash(git diff:*), Bash(git log:*), Bash(git stash:*), Bash(git add:*), Bash(git commit:*), Read, Edit(//Users/dan_personal/Projects/crude-tanker-fv/inputs/**), Edit(//Users/dan_personal/Projects/crude-tanker-fv/decisions/**), Write(//Users/dan_personal/Projects/crude-tanker-fv/decisions/**), Edit(//Users/dan_personal/Projects/crude-tanker-fv/PLAN.md), Write(//Users/dan_personal/Projects/crude-tanker-fv/state/fork_page.md), WebFetch(domain:www.sec.gov), WebFetch(domain:data.sec.gov), WebFetch(domain:www.globenewswire.com), WebFetch(domain:mfn.se), WebFetch(domain:newsweb.oslobors.no)
---

<!-- HOW TO INSTALL (owner, at the Mac): the desktop app's auto-mode classifier refused to let the
     agent register this task on 2026-09-13 (it registers other tasks fine; this one edits inputs
     and commits on a schedule, which the classifier treats as owner-only). Create it from the
     app's Scheduled section: New task → id crude-fv-fork-executor → schedule daily 12:45 → paste
     everything below the frontmatter as the prompt, then paste the allowed-tools line above into
     the task file's frontmatter (~/.claude/scheduled-tasks/crude-fv-fork-executor/SKILL.md).
     Until it exists, forks past their window sit un-executed; the sentinel keeps paging
     FORK-EXECUTABLE for them. -->

Fork executor for the crude-tanker-fv producer. Policy (owner, 2026-09-10): a registered recommendation EXECUTES after its silence window. Until 2026-09-13 the executing agent was always a chat session, so nothing executed without the owner opening one. You are that executor now, with BOUNDED authority: you execute a recommendation exactly as registered, you verify the result against what its record predicted, and you halt and page the owner on any miss. You never invent a different answer.

Repo: /Users/dan_personal/Projects/crude-tanker-fv (run everything from there). Read CLAUDE.md first (the verification loop, the price-basis rule, the provenance rule), then the fork registry header in inputs/forks.yaml.

TOOL DISCIPLINE (a permission prompt no human answers ABORTS the run): Bash is allowed ONLY for these forms: `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.forks ...` (list / mark), `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.rebase ...`, `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.drift_gate` (read-only, never --ratify), `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.reconcile <TICKER>`, `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.notify --send page ...`, `PYTHONPATH=src .venv/bin/python -m pytest ...`, `bash scripts/regen.sh <QUARTER>`, `git status`, `git diff`, `git log`, `git stash`, `git add <files>`, `git commit -m "<plain text, no backticks>"`. No push (the morning cron pushes). Read with the Read tool; write with Edit/Write under inputs/, decisions/, PLAN.md only. NEVER run scripts/ratify_baseline.sh and never `drift_gate --ratify`: the baseline is landed by the morning auto-land lane once the moved rows are annotated. NEVER edit .claude/settings.json, scripts/, src/, tests/ (a recommendation that needs a code change is halted with the reason).

Steps:
1. `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.forks list` — the open forks whose execute_after has passed. If "0 fork(s) executable today", report "nothing to execute" and stop. `git status` must show a tree that is clean apart from drift-list paths (inputs/market_data/prices_daily.yaml, inputs/research_pareto/_manifest.json, outputs/refresh_checklist.md, ...); if src/ or inputs/ carry OTHER uncommitted changes, stop and report "tree mid-surgery" — do not stash someone else's work.
2. For each executable fork, in registry order, ONE AT A TIME (finish, commit, then the next):
   a. Read its `doc` in full. Find the recommendation's exact edits (which file, which field, which value, which citation) and its PREDICTED IMPACT (a NAV / EV / band range, or "ΔNAV 0.0"). If the doc pre-registers no prediction, the prediction is: no gate row changes status except the named tickers.
   b. Kind `buyflip_*`: nothing to edit — the recommendation is "let the morning lane land the flip". Mark it executed (`forks mark <id> executed --note "silence ran out; the next auto-land ratifies"`), commit inputs/forks.yaml, continue.
   c. Apply the edits with the Edit tool, exactly as registered, each with its citation in the file's comment style. A watchlist pair rebase goes through `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.rebase <TICKER> --apply` (anchor-preserving; never type a price). A recommendation that says "settle X at the issuer source" means: fetch the source (WebFetch on the allowlisted issuer / EDGAR domains), quote it, apply what it settles; if the source does not settle it, mark the fork `halted` with the reason and page.
   d. If the edits touch a NAV determinant (inputs/ other than docs), commit the edits first (`git add <the edited files>` + `git commit -m "fork <id>: <one line>"` — regen.sh refuses a dirty determinant tree), then `bash scripts/regen.sh <current book quarter>`, then `PYTHONPATH=src .venv/bin/python -m pytest -q` (the whole suite) and `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.drift_gate`.
   e. VERIFY against the prediction: the gate's moved rows must be the named tickers and their NAV/EV moves must sit inside the pre-registered band (or be exactly 0.0 where "ΔNAV 0.0" was promised). Any other row moving >2pp, any band flip not predicted, any test red = a MISS.
   f. On a HIT: for every moved ticker, prepend a dated entry at the TOP of decisions/<ticker>_log.md in the log's style ("## YYYY-MM-DD — <EVENT> (fork <id>): ..." + a "**Decision:**" paragraph citing the doc and the observed vs predicted move) — the morning auto-land composes its cause from it. Mark the fork executed with the commit hash (`forks mark <id> executed --note "commit <sha> — <observed vs predicted>"`), then `git add` the outputs, the decision logs and inputs/forks.yaml and commit: "fork <id> executed: <observed move> (predicted <band>)".
   g. On a MISS: `git stash` the regen's uncommitted outputs (outputs/, decisions/*_log.md auto entries) so the tree is clean, mark the fork `halted --note "<what missed>"`, commit inputs/forks.yaml, and PAGE the owner: write /Users/dan_personal/Projects/crude-tanker-fv/state/fork_page.md with first line "# fork <id> HALTED — <what missed>" and a body ending "ACTION: OWNER — open a chat in crude-tanker-fv and rule on fork <id> (<doc>)", then `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.notify --send page --body-file /Users/dan_personal/Projects/crude-tanker-fv/state/fork_page.md --state-dir state`. Stop after a halt; leave the remaining forks for tomorrow.
3. Final report: a table of fork id, verdict (executed / halted / blocked), observed vs predicted, commit. If anything was halted, the first line is "OWNER ACTION NEEDED: fork <id> — <why>".

Never fabricate a citation or a number. A fork you cannot execute faithfully is halted with the reason, never approximated.
