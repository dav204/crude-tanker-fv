---
name: crude-fv-fork-executor
description: Daily 12:45: absorb the automation drift, absorb the inherited tape (annotate + auto-land, owner ruling 2026-09-18), run the ruled dry-FFA promote, then execute every open fork past its silence window (policy 2026-09-10). Bounded: exact edits per the fork's record, regen, gate, predicted-band check, suite, annotate, commit; halts and pages the owner on any miss; never hand-ratifies.
allowed-tools: Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.forks:*), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.annotate:*), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.promote:*), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.rebase:*), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.ffa_promote:*), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.drift_gate), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.reconcile:*), Bash(PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.notify --send page:*), Bash(PYTHONPATH=src .venv/bin/python -m pytest:*), Bash(bash scripts/regen.sh:*), Bash(./scripts/commit_drift.sh), Bash(git status:*), Bash(git diff:*), Bash(git log:*), Bash(git stash:*), Bash(git add:*), Bash(git commit:*), Read, Edit(//Users/dan_personal/Projects/crude-tanker-fv/inputs/**), Edit(//Users/dan_personal/Projects/crude-tanker-fv/decisions/**), Write(//Users/dan_personal/Projects/crude-tanker-fv/decisions/**), Edit(//Users/dan_personal/Projects/crude-tanker-fv/PLAN.md), Write(//Users/dan_personal/Projects/crude-tanker-fv/state/fork_page.md), WebFetch(domain:www.sec.gov), WebFetch(domain:data.sec.gov), WebFetch(domain:www.globenewswire.com), WebFetch(domain:mfn.se), WebFetch(domain:newsweb.oslobors.no)
---

<!-- HOW TO INSTALL (owner, at the Mac): the desktop app's auto-mode classifier refused to let the
     agent register this task on 2026-09-13 (it registers other tasks fine; this one edits inputs
     and commits on a schedule, which the classifier treats as owner-only). Create it from the
     app's Scheduled section: New task → id crude-fv-fork-executor → schedule daily 12:45 → paste
     everything below the frontmatter as the prompt, then paste the allowed-tools line above into
     the task file's frontmatter (~/.claude/scheduled-tasks/crude-fv-fork-executor/SKILL.md).
     Until it exists, forks past their window sit un-executed; the sentinel keeps listing
     FORK-EXECUTABLE for them in the digest. -->

Fork executor for the crude-tanker-fv producer. Policy (owner, 2026-09-10): a registered recommendation EXECUTES after its silence window. Until 2026-09-13 the executing agent was always a chat session, so nothing executed without the owner opening one. You are that executor now, with BOUNDED authority: you execute a recommendation exactly as registered, you verify the result against what its record predicted, and you halt and page the owner on any miss. You never invent a different answer.

Repo: /Users/dan_personal/Projects/crude-tanker-fv (run everything from there). Read CLAUDE.md first (the verification loop, the price-basis rule, the provenance rule), then the fork registry header in inputs/forks.yaml.

TOOL DISCIPLINE (a permission prompt no human answers ABORTS the run): Bash is allowed ONLY for these forms: `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.forks ...` (list / mark), `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.annotate --apply`, `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.promote land [--dry-run]`, `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.rebase ...`, `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.drift_gate` (read-only, never --ratify), `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.reconcile <TICKER>`, `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.notify --send page ...`, `PYTHONPATH=src .venv/bin/python -m pytest ...`, `bash scripts/regen.sh <QUARTER>`, `git status`, `git diff`, `git log`, `git stash`, `git add <files>`, `git commit -m "<plain text, no backticks>"`. No push (the morning cron pushes). Read with the Read tool; write with Edit/Write under inputs/, decisions/, PLAN.md only. NEVER run scripts/ratify_baseline.sh by hand and never `drift_gate --ratify`. The baseline moves ONLY through `promote land`, which re-ratifies behind its own five preconditions and writes its own commit — owner ruling 2026-09-18 put that call in step 0b; before it, the morning lane's alone. NEVER edit .claude/settings.json, scripts/, src/, tests/ (a recommendation that needs a code change is halted with the reason).

EVERY commit this task makes starts with `fork-exec` — the graph's R6 check maps that prefix to this node's declared writes, so a differently-prefixed commit would escape the check.

Steps:
0a. ABSORB THE AUTOMATION DRIFT FIRST. `git status --porcelain`. The launchd jobs dirty the drift-list files every day (inputs/market_data/prices_daily.yaml, inputs/market_data/transactions/_scan_state.json, inputs/research_pareto/_manifest.json, outputs/sp_print_candidates.md, outputs/ffa_ocr_queue.md, outputs/refresh_checklist.md, inputs/filings/_manifest.json) and `scripts/regen.sh` REFUSES any modified path under src/ or inputs/ — drift-list paths included — so a regen cannot run until they are committed. If any of them is dirty, run `./scripts/commit_drift.sh` (it stages exactly that list and commits "chore(automation): flush data/output drift"). This is also the CLAUDE.md price-basis rule: the price vintage is absorbed in its OWN commit before any sourcing regen, never bundled into one. If `git status --porcelain` still shows anything under src/ or inputs/ afterwards, stop and report "tree mid-surgery" — do not stash someone else's work.
0b. ABSORB THE INHERITED TAPE FIRST (owner ruling 2026-09-18: "let the executor annotate and land before judging the suite"). The drift gate measures from the RATIFIED ANCHOR, not from your promote, so any tape that landed since the anchor — a price vintage the 11:15 lane could not absorb because a chat's edits were dirty, a weekend, a holiday — appears as UNEXPLAINED rows against YOUR fork and reds the suite on drift you did not cause. That was three of the five halts between 2026-09-14 and 2026-09-18. Clear it BEFORE you touch any determinant, because the annotator's determinant proof refuses any window in which a curve moved: the moment step 0 lands an FFA curve this is no longer possible. Sequence — the 11:15 price-leg lane's own proven order (scripts/sentinel_cron.sh steps 3-6):
   i.   `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.promote land --dry-run` and read the five preconditions. Already AUTO-ABSORBABLE: skip to (iv). NOTHING TO LAND: go to step 0.
   ii.  If (e) says the committed surface is not current for HEAD, regen so the gate is awake: `bash scripts/regen.sh <the quarter in state/last_run.json>`.
   iii. `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.annotate --apply`. It may say exactly one thing — the tape moved, the valuation did not — and REFUSES every row whose fair value moved, naming the reason. A refusal is the design working, NOT an error: leave that row alone, never hand-write its entry, and carry the reason into your final report. Then, if (ii) ran, commit what it produced: `git add -u -- outputs decisions` and `git commit -m "fork-exec: absorb the inherited tape before the fork round - surface + gate annotations"`.
   iv.  `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.promote land`. It re-ratifies ONLY when all five preconditions hold and writes its own "baseline: auto-land" commit. A FREEZE is a correct outcome: a row still needs a human cause, so proceed with the anchor where it is.
   This step never touches a fork's edits and never ratifies YOUR OWN promote. The ordering law (comparator -> ratify -> suite, autopilot ruling 2026-09-02 §3.3) governs the fork's own move, which is still compared at 2e BEFORE anything of yours is anchored. Report the anchor before and after, the rows the annotator cleared, and every row it refused with its reason.
0. DRY FFA PROMOTE (mechanically determined under the owner's 2026-09-02 rulings — Q-1 unrounded 12M proxy, Q-2 the straddling shape, Q-3 no per-leg cap, Q-7 a cycle-band crossing is the ONE freeze). Run `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.ffa_promote --packet-out decisions/ffa_promotion_<today>.md`. If it prints `FREEZE: ...` (no promotable print, an unruled panel shape, or a band crossing), record that one line in your final report and go to step 1 — a freeze is a correct outcome, never a reason to hand-construct the curve. Otherwise read the packet, then `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.ffa_promote --apply --packet-out decisions/ffa_promotion_<today>.md`, `git add inputs/market_data/ffa_forward_curve.yaml inputs/market_data/twelve_month_tc.yaml decisions/ffa_promotion_<today>.md` and commit "fork-exec: PROMOTE dry FFA <print date> — INPUTS (packet decisions/ffa_promotion_<today>.md)". Then run the verification of step 2d/2e with this expectation: ONLY dry-bulk names move, every band label in the packet is unchanged, no test is red. On a miss, `git revert` is NOT yours — stash nothing, page the owner exactly as in 2g with "the dry FFA promote regen missed" and stop the run.
1. `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.forks list` — the open forks whose execute_after has passed. If "0 fork(s) executable today" AND step 0 froze or found nothing, report "nothing to execute" and stop. A fork flagged `needs_code: true` is NOT listed as executable (the registry hides it from you) and is never yours: its recommendation includes a src/ or tests/ change, the sentinel told the owner so when it opened, and a chat lands it. If `list` prints a "NEED A CHAT" line, repeat it in your final report and move on. If, while executing a fork that is NOT flagged, you discover the recommendation needs a code change, halt it as in 2g AND flag it: `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.forks needs-code <id> --note "<what code>"` before the commit, so the sentinel's next line says NEEDS A CHAT instead of paging a halt again.
2. For each executable fork, in registry order, ONE AT A TIME (finish, commit, then the next):
   a. Read its `doc` in full. Find the recommendation's exact edits (which file, which field, which value, which citation) and its PREDICTED IMPACT (a NAV / EV / band range, or "ΔNAV 0.0"). If the doc pre-registers no prediction, the prediction is: no gate row changes status except the named tickers.
   b. Kind `buyflip_*`: nothing to edit — the recommendation is "let the morning lane land the flip". Mark it executed (`forks mark <id> executed --note "silence ran out; the next auto-land ratifies"`), commit inputs/forks.yaml, continue.
   c. Apply the edits with the Edit tool, exactly as registered, each with its citation in the file's comment style. A watchlist pair rebase goes through `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.rebase <TICKER> --apply` (anchor-preserving; never type a price). A recommendation that says "settle X at the issuer source" means: fetch the source (WebFetch on the allowlisted issuer / EDGAR domains), quote it, apply what it settles; if the source does not settle it, mark the fork `halted` with the reason and page.
   d. If the edits touch a NAV determinant (inputs/ other than docs), commit the edits first (`git add <the edited files>` + `git commit -m "fork-exec <id>: <one line>"` — regen.sh refuses a dirty determinant tree), then `bash scripts/regen.sh <current book quarter>`, then `PYTHONPATH=src .venv/bin/python -m pytest -q` (the whole suite) and `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.drift_gate`.
   e. VERIFY against the prediction: the gate's moved rows must be the named tickers and their NAV/EV moves must sit inside the pre-registered band (or be exactly 0.0 where "ΔNAV 0.0" was promised). Any other row moving >2pp, any band flip not predicted, any test red = a MISS — EXCEPT tests/test_drift_gate.py::test_live_drift_gate_no_unexplained_drift, which is EXEMPT here (autopilot ruling 2026-09-02 §3.1 step 7 and §5): you read the gate DIRECTLY in this step against the frozen expectation, so the live-gate test only re-asserts the same rows and would red on tape step 0b could not clear. Every other test red is still a MISS.
   f. On a HIT: hand-write an entry ONLY for the tickers this fork names. A row that moved on tape alone belongs to the annotator (step 0b iii), never to you — a hand-written entry reads to the drift gate as a HUMAN annotation, so writing one for a row you did not cause would bless an unreviewed move. For every moved ticker THE FORK NAMES, prepend a dated entry at the TOP of decisions/<ticker>_log.md in the log's style ("## YYYY-MM-DD — <EVENT> (fork <id>): ..." + a "**Decision:**" paragraph citing the doc and the observed vs predicted move) — the morning auto-land composes its cause from it. Mark the fork executed with the commit hash (`forks mark <id> executed --note "commit <sha> — <observed vs predicted>"`), then `git add` the outputs, the decision logs and inputs/forks.yaml and commit: "fork-exec <id> executed: <observed move> (predicted <band>)".
   g. On a MISS: `git stash` the regen's uncommitted outputs (outputs/, decisions/*_log.md auto entries) so the tree is clean, mark the fork `halted --note "<what missed>"`, commit inputs/forks.yaml, and PAGE the owner: write /Users/dan_personal/Projects/crude-tanker-fv/state/fork_page.md with first line "# fork <id> HALTED — <what missed>" and a body ending "ACTION: OWNER — open a chat in crude-tanker-fv and rule on fork <id> (<doc>)", then `PYTHONPATH=src .venv/bin/python -m crude_tanker_fv.notify --send page --body-file /Users/dan_personal/Projects/crude-tanker-fv/state/fork_page.md --state-dir state`. Stop after a halt; leave the remaining forks for tomorrow.
3. Final report: a table of fork id, verdict (executed / halted / blocked), observed vs predicted, commit. If anything was halted, the first line is "OWNER ACTION NEEDED: fork <id> — <why>".

Never fabricate a citation or a number. A fork you cannot execute faithfully is halted with the reason, never approximated.
