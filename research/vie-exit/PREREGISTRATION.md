# VIE exit trial — frozen before replacement valuations

Registered 2026-09-24. Baseline identities and file hashes: frozen_baseline.json.
The user approved a $1,500 annual total replacement budget and excluded community
and commentary value. Budget is a target, not purchase permission. No provider is
selected. No real replacement observations exist at registration.

Production inputs, scenario weights, cycle bands, accepted publications, portfolio
decisions and governor baselines remain unchanged. This is an operational source
trial. No switch or cancellation is authorised by a successful shadow run.

## Contract and comparison rules

The screenshot labels Cape/Pmax/Smax are not benchmark identities. The inspected
2026-09-23 original has Sep, Oct, Q4, Q1 and Cal27 and no named benchmark version,
vessel specifications, price convention or timezone. Filename time is not a
verified market observation time. Those fields remain unknown and block matching.
Confirm USD/day and Capesize/Panamax/Supramax basket versions from provider
evidence; do not assume 4TC/5TC or 10TC/11TC equivalence.

Normalized records retain provider, panel, contract identity, vessel specification,
source date, timezone-aware observation and retrieval times, units, quote basis,
original tenor, explicit period, raw SHA256 and construction provenance. Revisions
are immutable snapshots; contradictory same-time quotes block use. Missing values
are unavailable, never zero. Holiday schedules need provider evidence.

Construct eight quarters using the existing calendar arithmetic, ruled tail steps,
two-quarter proxy and mappings (Post-Panamax=Pana; Handy-Bulk=Supra*0.90). Select
calendar coverage from richer exports, not positional rows. Unsupported fractional
rates block until an explicit rounding convention is reviewed. Quoted periods must
remain quoted, constructed periods derived. No hidden benchmark conversion.

Run 20 consecutive weekdays spanning month-end; identify provider holidays
separately. Require complete usable curves on at least 19 eligible days. All
missing expected prints, unresolved revisions, stale quotes and retrieval failures
remain visible. No calendar or trial window may be reset to discard bad days.
At least one genuine scheduled ingestion must complete without approval. Owner
economic gates do not count as operational failures.

Compare identical contracts/periods and observation times separately from timing
differences. Existing quote continuity (>10%) and valuation drift (>2pp EV/NAV),
band flips and k_broker second-difference >0.05 trigger investigation, not automatic
acceptance. No retrospective widening. Every changed governor restriction needs
explanation. Identical normalized data must produce identical curves and economic
outputs. Broker-diagnostic availability must not alter independent NAV/FV.

## Broker and P/NAV experiment

Backfill 90 days of independently acquired MB and harvested reports; retain
dispositions beyond this window. Extract cited candidate evidence, not accepted
transactions. Exact document/text duplicates share identity. Similar descriptions
remain possible duplicates until matched to a cited vessel/date/transaction; never
count publishers as independent corroboration automatically. Unsupported tables,
image-only pages and parse failures remain pending with a repair action.

Audit 90 days of broker diagnostic history. Record useful unique investigations,
duplicates, stale-vintage artifacts, noise and unclassified cases. No short-window
return attribution. Removing broker inputs makes broker checks UNAVAILABLE; it
does not make SANITY or drift pass. No production guard is disabled. A separate
purchase requires documented unique value, usable coverage and remaining budget.

## Decision

Retain temporarily until benchmark identity, licensed affordable access, the full
observed trial, diagnostic-control review and cancellation terms are evidenced.
Extend if incomplete; recommend exit only after all gates pass and owner reviews
full-book effects. Purchase, source activation and cancellation each need owner
approval. Unknown renewal notice terms block a verified decision deadline.

Rollback: set trial.json enabled=false; existing sources/publications remain
authoritative. Keep shadow receipts and archives. Re-enable without resetting the
trial window or deleting failures.
