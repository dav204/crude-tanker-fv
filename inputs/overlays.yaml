# Overlay ledger — curated source (METHODOLOGY §16).
#
# One row per ACTIVE qualitative overlay that sits between pipeline FV and
# the actionable view. §15 governance haircuts are NOT listed here — they
# auto-populate into the rendered ledger from balance_sheets/*.yaml
# governance_discount_pct (the YAML that actually drives the pipeline).
#
# direction: up = tool FV understates the actionable view; down = overstates.
# magnitude: a rough number/range when defensible, else "qualitative".
# scope: name-level rows use the ticker; sector-wide rows use the sector key.

overlays:
  - name: NAT
    overlay_id: "§12"
    direction: up
    magnitude: "qualitative — treat tool FV as the NAV floor, not the call"
    applied: 2026-06-05
    retire_trigger: "payout window closes (cycle normalization) or payout ratio drops below ~90%"
    note: "High-payout pure-play at peak; the dividend stream IS the thesis the strip undercounts."

  - name: SBLK
    overlay_id: "§12"
    direction: up
    magnitude: "qualitative"
    applied: 2026-06-09
    retire_trigger: "dry-bulk cycle position back inside ~1.2x of anchor"
    note: "Peak dry bulk per §12.2; tool TRIM at band-edge — read §12 before acting on the short side."

  - name: DHT
    overlay_id: "§14.4"
    direction: up
    magnitude: "residual only under Jun-9 weights (see §14.4 double-count warning)"
    applied: 2026-06-03
    retire_trigger: "MEG export volumes confirmed ramping in step with transit, or Hormuz physical resolution re-bases weights"
    note: "High-MEG VLCC exposure; full +10-15%/+5-10% adjustment applies ONLY under v1-style normalization-leaning weights."

  - name: ECO
    overlay_id: "§14.4"
    direction: up
    magnitude: "residual only under Jun-9 weights (see §14.4 double-count warning)"
    applied: 2026-06-03
    retire_trigger: "same as DHT §14.4 row"
    note: "All-spot, MEG-routed; near-term TRIM signals may resolve opposite over 1-2 quarters."

  - name: FRO
    overlay_id: "§14.4"
    direction: up
    magnitude: "residual only under Jun-9 weights (see §14.4 double-count warning)"
    applied: 2026-06-03
    retire_trigger: "same as DHT §14.4 row"
    note: "High-spot VLCC/Suezmax MEG exposure."

  - name: STNG
    overlay_id: "§14.6.1"
    direction: up
    magnitude: "qualitative — uncaptured clean/dirty switching option on 32 coated LR2s"
    applied: 2026-06-05
    retire_trigger: "clean-dirty LR2 spread normalizes, or the option is modeled (backlog)"
    note: "Framework routes STNG LR2s through lr2_clean only; the embedded switch option is invisible to the strip."

  - name: crude
    overlay_id: "§14.6.2"
    direction: down
    magnitude: "qualitative, date-specific binary"
    applied: 2026-06-03
    retire_trigger: "waiver expiry date passes or is renewed"
    note: "Sanction-waiver expiry; sector-wide event risk on Iran-adjacent crude flows."

  - name: MPCC
    overlay_id: "§11.8.5(b)"
    direction: down
    magnitude: "unquantified — marks-vintage premium on aged feeder tonnage"
    applied: 2026-06-11
    retire_trigger: "fresh MB assessment set lands (direct subscription) and old-age leg re-fit"
    note: "Boom-flat old-age curve at a 10-week-stale vintage, concentrated in old feeders; no external NAV anchor to catch it. First container ledger row, per §11.8.5."
