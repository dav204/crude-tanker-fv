# WF Artemis G3-retroactivity — the WHAT-IF, computed (2026-08-09; ruling stays the owner's)

**The question (open since 7/28 §H):** WF Artemis (Supra-Ultra, 2020 Iwagi, $36.5M,
promoted 7/18 as `standard`/in-fit) is a PROMPT-DELIVERY print per Splash247 — under the
7/28 `prompt_premium` rule it would have entered `in_fit: false`. Retro-flag it?

## Solver dry-run (Thread-1 pattern — computed on the in-memory print set, repo untouched)

| | n | 5yr anchor | 10yr anchor | slope |
|---|---|---|---|---|
| As-is (WF Artemis in-fit) | 44 | **$30.82M** | **$24.47M** | −$1.272M/yr |
| Retro-flagged (in_fit false) | 43 | **$29.70M** | **$23.93M** | −$1.156M/yr |
| Δ | | **−3.63%** | **−2.21%** | |

(Current wired anchors for reference: 5yr $33.0M / 10yr $25.0M — the fit re-solves per
run; the table isolates the WF-Artemis-only effect on today's 44-print set.)

## Read

- The effect is REAL but modest and one-directional: the prompt-premium print props the
  Supra level by ~2-4% at both nodes. Retro-flagging is the G3-consistent treatment
  (same basis as SEACON TOKYO / the Oldendorff pair / CMB Jordaens — all flagged at 7/28;
  WF Artemis escaped only by entering ten days earlier).
- **Interaction the ruling should see: SBLK sits FROZEN at the BUY/HOLD boundary**
  (the 8/09 composition flip). A −2-4% Supra anchor move shaves SBLK NAV further —
  ruling this WITH the freeze disposition keeps one SBLK story, not two.
- Consistency rider if ruled YES: WF Artemis is the only KNOWN pre-flag-vintage prompt
  print, but a one-time sweep of the 7/18 batch for the same pattern (prompt-delivery
  noted in source prose, standard flag) should ride the same commit — else the
  vocabulary applies from 7/28 for some hulls and 7/06 for one.
- If ruled NO (vintage-of-entry stands): record that flags apply from their adoption
  date forward, and strike the retroactivity line from the docket.

Execution if ruled YES: flip `in_fit` + `quality_flag: prompt_premium` on the row with
a dated note → prints-drift loop → annotate SBLK/SB → ratify. ~15 minutes, fits any
session; sequencing-wise it should NOT ride the Stage-A regen (one FV-moving event at
a time — it is its own small event).

---

## RULED + EXECUTED (2026-08-09, owner: "rule yes, and rule it first")

- WF Artemis -> `prompt_premium` / `in_fit: false` (supra_ultra.yaml, dated note).
- **The pre-7/28 aggregate sweep the ruling asked for: NO other fit-moving case.**
  Jag Lokesh (LR2, drydock-due = impairment-side, wrong channel + wrong class) ·
  Wooyang Belos (promoted AT the 7/28 G3 sitting, considered, age 10, standard is
  right) · Singapore Spirit (VLCC issuer realization, no channel) · Nord pair
  (G3-considered 7/28, Kamsarmax at DISCOUNT to NB - deliberately unflagged) ·
  young Cape packages (forward-delivery, §H precedent != prompt). ONE same-shape
  hygiene case found and fixed in the same commit: **Vulcania (Pana 6/12) standard
  -> tc_attached** — the flag postdated the row; 0.0 uplift, stays in-fit, ZERO
  anchor movement.
- Run result: Supra anchors moved as dry-run predicted; SBLK NAV -0.8% / EV
  +3.8% -> +3.1% (sub-threshold, gate 0 UNEXPLAINED); SB exactly 0.0 (no
  Ultramax exposure — Pana/PPMX/Cape book untouched). **SBLK freeze DISPOSED per
  the owner's pre-specified rule: the flip survives the corrected anchors ->
  real -> ratified HOLD** (sblk_log annotation same date).
- Band-note correction for the record: the k uniformity band was re-pinned
  (0.95, 1.15) at the 8/09 marks ratify and tests DHT/FRO/ECO — GNK is not a
  member; direction logic (dry k rises toward center) unaffected.
