# WO2 acceptance receipts — owner-edited; null = ❌ pending (close_acceptance.py joins these).
# Install receipts (2026-07-12/13, recorded at install close):
#   - email channel: notify --doctor ok + test mail SENT 2026-07-13 (NBSP scrub en route)
#   - dead-man: CRUDE_FV_HEALTHCHECK_URL fixed (uuid form) → sentinel PING-SENT 2026-07-13
#     09:52 ET (first successful ping; check green on healthchecks)
#   - flags→email join: the same 2026-07-13 run emitted 4 flags AND sent the digest (SMTP live)
#   - launchd: 8/8 rows loaded 2026-07-12 (D-2 closed); ctxprobe logging
#   - GitHub secret SENTINEL_LITE_HC_URL set 2026-07-13T00:57Z (write-only; proven by the
#     action_issue demo below)
#
# The three demonstrations below are DELIBERATE drills — schedule with the owner (the
# healthchecks firing = a 2-day ping gap, i.e., intentionally silencing the watcher):

healthchecks_firing_demonstrated: null   # 2-day ping gap → healthchecks pages by absence; record date + page screenshot ref
action_issue_demonstrated: "2026-07-13 workflow_dispatch run 29255810164 (jobs sentinel+ci green);
  created issue #5 'sentinel: 1 flag(s) on pushed state' (the doha TRIGGER-DUE — correctly read from
  the PUSHED state, which lagged local resolution; self-closes on the next quiet run after push).
  Lite healthcheck pinged by the Action run — paired-check identity confirmed vs the env-pinged
  sentinel check.
page_ack_demonstrated: null              # one PAGE → owner ack latency demo (NOT a standing SLA); record timestamps
clean_clone_suite: null                  # fresh-clone pytest transcript ref (known 3 machine-local skips)
attestation: null                        # owner sign-off line, dated
