<!-- PROVENANCE: drafted 2026-09-02 by a multi-agent audit workflow at owner request (2026-09-01, "happy to
discuss stopping / pruning pieces that aren't being used for anything in the valuation process"): 5 subsystem
auditors (fetch layer / sentinel+notify / outputs+diagnostics / registers+triggers / tests+docs) -> 5 refuters
(one per audit, tasked to rescue every prune candidate by naming a consumer) -> ledger synthesis. Read-only at
HEAD 4bb3237. Status update from the main session: fix-first item (1) — the archive_gaps.yaml parse failure — is
repaired in the working tree (uncommitted); item (2) verified: `ruff check` reports 2 unused imports in
scorecard.py and every sentinel-lite run since 8/31 shows `failure`. Companion:
decisions/autopilot_authority_2026-09-02.md (what to automate). RULED 2026-09-02 (owner verbatim: "accept all F recs except F7 no (keep), F18 no (keep)") — F1…F20 stand at their recommendations (F7 and F18 were recommended 'no' = keep, so the ruling accepts every recommendation); the fix-first items landed with Stage 0. -->

# Prune ledger — 2026-09-01 owner words ("stopping / pruning pieces that aren't being used for anything in the valuation process")

**Status:** DRAFT for owner ruling · **Authority:** owner words 2026-09-01 (automation ask + prune invitation) · **Inputs:** five subsystem audits (fetch, sentinel, outputs, registers, guards) + five refutations, read-only at HEAD `4bb3237`; every "today" claim below re-checked 2026-09-02 before dawn (no 9/02 sentinel run yet; tree dirty on `inputs/archive_gaps.yaml` + `prices_daily.yaml`). Nothing here has been executed.

## 0. TL;DR

**Fix first (not pruning — the machine is bleeding):** (1) `inputs/archive_gaps.yaml` at HEAD does not parse (line 86) and killed the 9/01 sentinel; the fix is already in the working tree uncommitted — commit it ALONE (not with the dirty `prices_daily.yaml`) and push, because `sentinel-lite` reads origin. Until then the 9/02 08:15 run goes META-MODE (tree dirty) and `escalation_pause_corroboration` (due 9/01) sits overdue unpaged. (2) CI has been red on every run since 7/24 on two unused imports in `scorecard.py:32,61` (`ruff --fix`); the clean-clone suite has not executed for six weeks. (3) `scripts/sentinel_cron.sh:41-44` maps rc=1 to `outcome=flags`, so a traceback reads as a normal flag day — make flags exit 2. (4) Add a five-line test that `yaml.safe_load`s every live YAML the sentinel reads. (5) `ingest_rocketchat_cron.sh` never rebuilds `_manifest.json`, so every weekday `sp_scan` runs on Saturday's index (the 9/01 "nothing to scan" false-clean) — add the indexer, dailies-root only. (6) Drop the summer `silence_days: 14` override (`rocketchat_sources.yaml:25`, "REVISIT ~Sep-1").

**Stop this week (no consumer, no owner ruling in the way):** ctxprobe plists (hourly zombie since 7/12, 1,044 log lines); `baltic_capesize_table` lane (crashed 8/28, wedged 7/24-27); `baltic_indexes` lane + parser (README itself says "deliberately unconsumed"); `draft_queue.py` (cannot run — no `claude` on PATH — 0/17 preregs used it); `DISK-PRESSURE` route (no emitter ever existed); `TRIGGER-EVIDENCE` lane (blind: newest hit 2026-03-03 while the owner read three Suezmax 1Y prints by eye); all 35 committed `.xlsx` (zero readers outside tmp-dir tests; 4,074 blob versions, 36.7 MB); `consensus_eps` stage; `ffa_vs_strip`; `pareto_mentions_*` ×20; `state/fdprobe` + `capt_*.txt`; the linked-Pareto-report weekly chain (0-9 unread PDFs/week, one citation ever).

**Downgrade / simplify:** the sentinel from ~33 daily flag-lines and ~20 pages/month to a Saturday digest plus 2-6 pages/month (FILING-LANDED page once, report-class only; STALE-BALANCE-SHEET and EARNINGS-DUE become one line each; mb:* collapsed); fold the five §9.10 family sidecars into a pipeline stage (19 re-run dates since 7/01 → 0); write the decision-log auto entry only for breaching names (5,042 machine headers vs 163 human); replace value-pin literals with one scorecard-agreement test (7 re-pin commits in 6 weeks → 0); `transaction_anchor_comparison` runs with the S&P round, not every regen; register 23 → ~10 cards; PLAN.md 824 → ≤150 lines.

**Leave alone (consumer named):** Pareto dailies + `sp_scan`, FFA lane, EDGAR/HKEX/NewsWeb pollers, price-refresh, MB weeklies, the harvester crawl (feeds the xclusiv age-0 NAV anchor and ~35% of promoted prints), `weight_robustness.yaml` (governance TRADE_PREREG rule 1), `book_scorecard.*`, `delta`/`last_run.json`, `<t>_fv_report.md`, `<t>_scenarios.md`, `broker_nav_sweep.md`, `justified_pnav.md`, all structural/identity guards, the trigger-register mechanism, `earnings_calendar` + sweep, `agent_duties`, healthchecks ping.

**What it removes:** roughly 2-3 owner-hours/month of digest reading and page triage; ~1.5-2 h/month of re-pin + sidecar-re-run + double-regen ritual in season; 30-60 owner-minutes per full week-close doc audit; ~50 red-CI emails/month; ~40 flag-lines/day of digest; ~700 lines in the sentinel subsystem, ~1,300 lines of dead tests/modules elsewhere, 35 binary blobs per regen, ~700 lines of PLAN. Machine time is not the argument anywhere — the pipeline is ~50 s (outputs audit §0.1, mtime-verified), the sentinel ~1 s.

## 1. Method + evidence standard

A piece is USED only if a named consumer exists today: a loader that reads it into a number, a `book_scorecard.json` field the governance repo reads, a decision doc that cites it as the basis of a ruling, or a guard with a recorded catch (CHANGELOG/decisions/commit subject). "Might matter someday" is not a consumer. Each auditor's verdict was refuted by a second agent with grep/`git log -S`/manifest recounts; where the refuter rescued an item (named a consumer the auditor missed) the ledger says KEEP with that consumer. Where auditor and refuter still disagree and I could not settle it from the repo, the row says UNVERIFIED and the item goes to §3e/§6 as an owner fork. Counts I re-verified this morning: 8 plists in `~/Library/LaunchAgents` (incl. 2 ctxprobe), 35 tracked xlsx, CLAUDE.md 15,960/16,000 chars, README claims 639+ tests (static census 639-640 defs), `weight_robustness.yaml` rewritten on 19 distinct dates, 8 done / 15 armed register cards, PLAN 824 lines, ruff 2 errors, HEAD `archive_gaps.yaml` fails parse / worktree copy parses.

## 2. THE LEDGER

Columns: item · subsystem · consumer (or NONE) · last mattered · cost (machine / owner-min per week / noise / surface) · verdict · removal steps · what is lost.

| # | Item | Sub | Consumer | Last mattered | Cost | Verdict | Removal steps | Lost |
|---|---|---|---|---|---|---|---|---|
| 1 | RC ingest: Pareto dailies + `sp_scan` | fetch | 107 transaction rows `source: Pareto`; watchlist pair (22 names, 8/28); `sp_promotion_round_2026-09-01`, `marks_trail_triage_2026-08-31` | 9/01 | ~1 min/day; owner 1 round/2 wks; `.err` urllib3 noise | **KEEP + fix** | Add `pareto_archive --build-manifest` (dailies root only, `ARCHIVE_ROOTS=(ARCHIVE,)`) before `sp_scan` in `ingest_rocketchat_cron.sh` | — |
| 2 | FFA screenshot lane + `ffa_ocr` | fetch | 4 FFA promotions → `ffa_forward_curve.yaml` → dry 12M TC; sentinel `UNINGESTED-PRINTS ffa` reads `state/ffa_ocr_curves.json` | 8/31 | 43/88 OCR rows flagged; month-end grid always "incomplete" | **KEEP; SIMPLIFY OCR** | Teach `ffa_ocr.py:179-180` the 4-tenor month-end grid (already owner-backlogged, `ffa_capture_review_2026-08-31.md:25`); keep `--staleness` | — |
| 3 | `inputs/ffa_drybulk/2017..2024` (11,831 images, ~1.5 GB, gitignored) | fetch | NONE (curves json = 2026 only; backtest vintages carry their own yaml) | never | disk only | **ON-DEMAND / owner fork F7** | `rm -r` — NOT git-reversible; RC `--backfill` re-pull UNVERIFIED | Only visual record of 2017-25 dry FFA curves |
| 4 | `baltic_indexes` lane + `baltic_parser.py` + CSV | fetch | NONE — `normal_rates.py:206,271` refuses it; README:349 "deliberately unconsumed"; METHODOLOGY:1625 claim is false | never | 22 drift commits; 7/24 rewrite; 7/22 false alarm | **PRUNE lane; freeze CSV** | Delete source block; `baltic_parser.py`, `tests/test_baltic_parser.py` (16), `kind=text` branch + caption concat `ingest_rocketchat.py:246-252`; `test_sentinel.py:319-341`; `test_cron_guard.py:83` re-pin; `drift_files.txt` line; fix METHODOLOGY:1625, README:349, PLAN:148 | A 769-row tape (2023-02 → 2026-08) stops growing; kept frozen in git |
| 5 | `baltic_capesize_table` PNG lane (CPLazos) | fetch | NONE — "staging-only, no consumer, owner ruling pending" (CHANGELOG 7/24); §18.5a gate ~2029 | never | crashed 8/28; wedge 7/24-27 | **PRUNE** | Delete source block; `rm -r inputs/baltic_capesize`; `.gitignore:62`; fix `_decide_oldest` regardless | 108 seed PNGs (RC re-pull UNVERIFIED) |
| 6 | Linked-report chain (`--links` → `fetch_links` → `research_pareto_other/linked`) + `pareto_daily_links.json` | fetch | One basis citation ever (`capt_log.md:3174`, May onboarding); `insw_log.md:3607` speculative | 2026-05 | 3-min manifest walk/Sat; 192 MB; 17+5 churn commits | **ON-DEMAND** | Remove steps 3-5 from `news_pull_cron.sh`; `git rm outputs/pareto_daily_links.json` + its `drift_files.txt` line; keep `fetch_links.py`/test/ask rule for onboarding; WORKFLOWS:181-182, 219-222 | Unread PDFs stop accumulating; onboarding = one manual `fetch_links` |
| 7 | `research_pareto_other` (366 files) | fetch | only `pareto_archive` indexes it | 2026-05 | disk | **DOWNGRADE** | Stop feeding (row 6); leave on disk | — |
| 8 | `shipping_harvester` crawl (Sat 09:00, `.venv310`) | fetch/outputs | **RESCUED**: `xclusiv_age_curve.yaml` (age-0 NAV anchor, LOCKED 6/29, `scorecard.py:812` resale-uniform) parsed from `data/marks/xclusiv`; 80 of 231 `source:` rows (advanced 29, xclusiv 24, banchero 17, intermodal 10); cited `thread_d_crude_level_confirmation_2026-07-15.md:29`, `mr_secondhand_resumption_2026-07-15.md:11` | 7/15 (anchor); 8/09 (triage) | ~2 min/Sat; already current-quarter-only (`harvester_cron.sh:39-43` — auditor's "8 quarters" was wrong); 195 MB | **KEEP crawl; DOWNGRADE footprint** | Tag fearnleys assessment-only in manifest; re-point `UNINGESTED-PRINTS marks-trail` (`sentinel.py:400-421`) at the MB staging dirs; do NOT `rm data/` (two decision citations) | — |
| 9 | Harvester CI job (`sentinel-lite.yml:101-112`, 57 tests on push + daily) | outputs | External-audit disposition N-5 (7/14, CHANGELOG:520); rationale now false (8/31 and 9/01 rounds cite harvester 0×) | 7/14 | CI minutes daily (no `if:` guard) | **PRUNE — owner fork F1** | Remove job; log an N-5 re-disposition line in CHANGELOG; or at minimum `if: github.event_name == 'push'` | Off-machine parser regression on push |
| 10 | EDGAR poller | fetch | 36/61 accessions cited in sheets/manifests/decisions | 9/01 | `.err` 1,959 lines (DNS week) | **KEEP** | — | — |
| 11 | HKEX poller | fetch | **4 of 11** used (Board-meeting date = formal 2343 results date `earnings_calendar.yaml:7`; interim results = Q2 sheet; interim report `watchlist.yaml:393`; Monthly Return = share-count watch) — auditor's 1/11 was wrong | 8/2026 | 9 FILING-LANDED pages | **KEEP; SIMPLIFY filter** | Add ToR / Corporate-Governance / "Circulars-[Other]" to the exclusion set (`hkex_poll.py:59-61`); keep the category (`test_hkex_poll.py:60-72` stays green) | — |
| 12 | NewsWeb poller | fetch | 2/14 cited; exists for 4 documented misses; allowlist is ruled out by `test_known_historical_misses_survive_the_filter` | 8/25 | 10 pages | **KEEP as-is** | Noise fix lives in row 21 (page once), not here | — |
| 13 | `draft_queue.py` + `tests/test_draft_queue.py` + 2.4b design | fetch/guards | NONE — no importer, no cron, no skill; `claude` not on PATH; 0/17 preregs | never | 40 LOC + doc debt | **PRUNE** | `git rm` both; move the 2.4b gating paragraph to CHANGELOG; edit WORKFLOWS:232, PLAN:282 ("draft queue work unchanged"); keep `/filing-packet` skill doc | A design note (preserved in CHANGELOG) |
| 14 | price-refresh (18:30) | fetch | `prices_daily.yaml` → `current_price` → read_flag/EV%/drift gate | daily | 8 days lost to the dirty-tree guard (failure h) | **KEEP** | F2 (2343/SB static fallback) is STALE — rebase landed `2d49d86` 8/31; the 8/31 sentinel echoed the pre-rebase flag. Rule only if the first post-rebase refresh still flags (UNVERIFIED) | — |
| 15 | MB weeklies (Gmail agent + `mb_harvest.py`) | fetch | 57 rows; container §11.8 source of record; 9/01 round's pana/supra prints | 9/01 | lapsed 3 wks in Aug (human step) | **KEEP** (automation target, not prune) | — | — |
| 16 | ctxprobe plists ×2 + `scripts/ctxprobe/` + `tests/test_ctxprobe.py` + `~/ctxprobe.log` | fetch/sentinel/guards | NONE live; `close_acceptance.py:127` reads the log (row 43, itself PRUNE); the one thing it proved is banked in `cron_lib.sh:30` | 7/03 | hourly curl to github.com for 7 wks; 1,044 lines; subprocess per pytest | **PRUNE — owner runs launchctl (fork F6)** | Owner: `launchctl unload` ×2 + `rm` per `ctxprobe_checklist_2026-07-03.md:41-45`; then `git rm -r scripts/ctxprobe tests/test_ctxprobe.py`; README:334 stops saying "measured" (checklist :50-57 all `_pending_`); optional 30-min log summary first | Nothing demonstrated |
| 17 | `state/fdprobe` (18 MB), `state/capt_*.txt` | fetch | NONE (CHANGELOG:1313 history only) | 6/22 | disk | **PRUNE** | `rm -r` (gitignored) | — |
| 18 | `state/ffa_scratch`, `state/test1_scratch` | fetch | **RESCUED**: `ffa_ocr.py:46` live tesseract scratch; Test-1 tmp_out | live | — | **KEEP** | strike from any prune list | — |
| 19 | `inputs/research_issuer`; `pareto_archive --extract` CSVs | fetch | citation store (BRUT/CAPT/TEN/2343 preregs); `backtest/loaders.py:22` | 8/13; 7/03 | zero automation | **KEEP / ON-DEMAND** | freeze the extract with the backtest (row 60) | — |
| 20 | Sentinel `UNINGESTED-PRINTS` + `FLEET-TRANSACTION` | sentinel | 9 decision docs name the flag as trigger (title of `marks_trail_triage_2026-08-31`); `sp_candidates_triage_2026-08-16` | 8/31 | 51+11 Aug lines; 14/14 days always-on | **KEEP; DOWNGRADE cadence + merge** | One "S&P queue" line at threshold crossing then weekly (7/28 spec item 4); keep `sp_scan --mark-reviewed` ack path; edit `sentinel.py:266-290,446-456` | — |
| 21 | `FILING-LANDED` (page, 48 h window) | sentinel | 8 ticker-log triage entries (all "no ingest"); SBLK SPA zombie 8/11; **FLNG 8/19 (the one that needed a page) never paged** (`grep -c "FILING-LANDED FLNG" sentinel.log` = 0) | 8/11 | 94 Aug lines = 61 arrivals × 2 days; 12 of 33 on 8/31 | **DOWNGRADE** | Page once (cursor "arrivals since last non-META run", `sentinel.py:674-686`), only report-class in an open window — classify EDGAR by `primary_doc` name (`*-20260630.htm`), NOT exhibits (no manifest row carries `exhibits`); rest → weekly line; `test_sentinel.py:531-539` stays green | — |
| 22 | `TRIGGER-DUE` (page) | sentinel | Doha 7/12 → war-tilt restore (largest valuation consequence of any tag); pause checks 8/09, 8/25 | 8/25 | 4 Aug lines | **KEEP, page first day only** | day-2 repeat → digest | — |
| 23 | `STALE-BALANCE-SHEET` | sentinel | NONE cited; Q2 sheets landed in sittings (7/31, 8/08, 8/10, 8/13, 8/25, 8/29, 8/31, 9/01) independent of the daily line | never | 77 Aug lines | **DOWNGRADE** to one weekly line "sheets missing: N" | note `refresh_checklist.md` is hand-run only — PLAN is the other surface | — |
| 24 | `EARNINGS-DUE` (14-day daily recitation) | sentinel/registers | 6 Q2 preregs cite the calendar, not the flag; `refresh.py:170` renders the same check; its one page (8/12 escalated) was meaningless | never as the daily line | 115 Aug lines (277 over 48 run-days) | **DOWNGRADE** to a one-line calendar strip | `sentinel.py:706-712,723-744`; if `EARNINGS_UPCOMING_DAYS` 14→3, re-pin `test_refresh.py:354-360` | — |
| 25 | `EARNINGS-UNCONFIRMED` (unrouted → pages daily) | sentinel | **Owner directive 7/21 `3fa799e`** ("window ≤10d + status=expected pages per-name"); 7/22 `f070cbe` sweep on the 5 pages → CCEC confirmed; 8/18 FLNG move caught by the sweep family, prereg frozen the night before | 8/18 | 38 Aug lines | **KEEP check; DOWNGRADE to page once per name** | Route explicitly in `notify.yaml` + `test_notify.py:47-52`; suffix on the strip line; first sighting pages (directive-consistent), not daily | — |
| 26 | `EARNINGS-SWEEP-STALE` (unrouted → page daily; trailing colon) | sentinel | Same directive (b); 8/31 `ce41e80`; 12 daily pages → 3 sweeps | 8/31 | 12 Aug pages | **DOWNGRADE — fork F11** | Daily digest, page once at >7d in season (refuter form) vs Saturday-only (auditor); strip the colon; route explicitly | Saturday-only might have missed FLNG (UNVERIFIABLE) |
| 27 | `STALE-INPUT` mb:* + `check_market_data` mtime lane | sentinel/registers | mb:* → `marks_trail_triage_2026-08-31.md:6`; mtime lane fired 2× (`historical_tce_means`, since removed from the file list), acted on 0× | 8/31 | 66 Aug lines (53 = four mb lanes saying one thing) | **KEEP, collapse; PRUNE the sentinel mtime call** | One `mb:` line, weekly re-promotion; delete `sentinel.py:101-104` (keep in `refresh`) | — |
| 28 | `FETCH-FAILED` | sentinel | Two catches (7/13 harvester never installed; 7/27 RC wedge, CHANGELOG:294) — both via the `escalate_after: 2` override, not the in-window page promotion; DNS-week pages were undeliverable by construction | 7/27 | 16 Aug lines | **KEEP digest + threshold; PRUNE the page promotion** | delete `sentinel.py:880-883`; re-pin `test_fetch_failed_promotes_to_page_in_open_window` | — |
| 29 | `PRICE-BASIS`, `DIRTY-TOO-LONG`, `ARCHIVE-GAP`, `AGENT-TASK-DUE`, `FILING-OVERDUE`, `NOTIFY-DOWN`, guard tags (SURFACE-INCOHERENT / SIDECAR-STALE / NOTIFY-UNCONFIGURED / CALENDAR-UNSEEDED) | sentinel | PLAN owner-owes; only tell the sentinel is blind; June 1-3 gap accepted 8/16 `102fe51`; 8/24 dead-task catch; `cmdb_log.md:867`; SIDECAR-STALE fired 0× (becomes tautology after row 47) | various | ~15 Aug lines total | **KEEP** (ARCHIVE-GAP weekly after the YAML fix) | — | — |
| 30 | `TRIGGER-EVIDENCE` lane (`sentinel.py:419-445`) + `sp_scan.extract_tanker_period_signals` | sentinel/registers | NONE effective — 11 hits, newest 2026-03-03; the 9/01 daily's third Suezmax 1Y print produced no match (`TANKER_CLASS_PATTERN ∧ TC_PERIOD_PATTERN`, table text runs together); the trigger was serviced by owner reading | never | 60 LOC + 2 tests | **PRUNE** (with row 68 after Stage B) | delete `sentinel.py:419-445`, `test_sentinel.py:420-449`, `sp_scan.py:561-580`, `test_sp_scan.py:60-84`, `notify.yaml:25`, `test_notify.py:49`, README:314, header comments `twelve_month_tc.yaml:22` / `ffa_forward_curve.yaml:26` | — |
| 31 | `DISK-PRESSURE` route (`notify.yaml:33`) | sentinel | NONE — no emitter ever existed (`git log -S` = the routing-table commit only) | never | one line | **PRUNE** | delete the line; `test_real_routing_table_covers_every_live_sentinel_tag` | — |
| 32 | Digest-streak escalation (`escalate_after`, `sentinel.py:894-909`) | sentinel | 3 escalated Aug pages, all always-on tags; META runs reset every streak | never usefully | mechanism + state key + test | **SIMPLIFY — fork F10** | Replace with weekly re-promotion (row 20); keep `FETCH-FAILED: 2` as a plain threshold; delete `escalated` state key, `test_digest_streak_escalates_once` | — |
| 33 | healthchecks dead-man ping | sentinel | Governance postmortem 8/09: owner "I don't see anything from healthchecks since mid july" — channel delivers, one email ever noticed; PING-SKIPPED 30 runs (drill left URL commented a month); receipts `healthchecks_firing_demonstrated: null` | never demonstrated | nil | **KEEP; stop claiming coverage** | Strike README:306-310 "notifier death is detectable" until one alert receipt is recorded — the 9/02-evening miss (no 9/01 ping) is the live test | — |
| 34 | `sentinel-lite` GitHub Action | sentinel | Issue #5 demo (7/13) = only off-machine TRIGGER-DUE path; #6 silently re-edited daily since 7/14; `ci` dead since 7/24; `--pure` FILING-LANDED reads a manifest last committed 8/13 | 7/13 | 3 jobs daily; ~50 red emails/month (email routing UNVERIFIED) | **DOWNGRADE — fork F9** | Fix the 2 imports; issue path comment-on-change; `ci`+`harvester` push-only (`if:`); sentinel job daily (refuter, 10 s) vs weekly (auditor) | — |
| 35 | `close_acceptance.py` + `tests/test_close_acceptance.py` + `wo2_acceptance_receipts.yaml` + pinggap drill doc | sentinel/guards | NONE — never run (`outputs/acceptance_*` absent); receipts 3/5 null; PLAN:257-258 stale | never | 215 + 60 lines; stale PLAN item | **PRUNE — fork F6** | `git rm` module/test/receipts; fold a two-line outcome into CHANGELOG ("drill never closed; URL restored 8/14"); keep `notify.py:87-92` send ledger; drop PLAN item 3 | A WO2 sign-off ritual nobody performed |
| 36 | Daily "All checks quiet" digest as liveness tell | sentinel | the owner's only daily proof the notifier is alive (row 33) | daily | 1 email/day | **KEEP as one line** until a healthchecks receipt is recorded | then weekly | — |
| 37 | `<t>_fv_report.md`, `<t>_scenarios.md`, `broker_nav_sweep.md`, `justified_pnav.md`, `delta_report.md` + `state/last_run.json`, `book_scorecard.*`, `RATIFY_LOG.md`, `news_digest_*` | outputs | q2_cluster_transition:79; sentinel `_scenario_doc_pw_fv`; `k_broker` → drift gate; §17 margin → `read_flag` (governance sizing); `drift_gate.py:67`; governance contract; `agent_duties` glob | live | — | **KEEP** | — | — |
| 38 | All 35 committed `.xlsx` (`<t>_fv_report` ×25, `fair_value_summary`, `scenario_summary`, `broker_nav_sweep`, `transaction_anchor_comparison`, `justified_pnav`, `consensus_eps_xref`, 3 family) | outputs | NONE outside tmp-dir tests (grep decisions/PLAN/CHANGELOG/WORKFLOWS/governance = 0; `sblk_log.md:4101` only notes a regen) | never | 1,219 file-touches in Aug; 4,074 blob versions = 36.7 MB of 331 | **PRUNE** | Skip `_render_xlsx` (`report.py:450-457`), remove `write_watchlist_summary`/`write_scenario_summary` calls (`pipeline.py:584,646`), `pipeline.py:801-819`, `justified_pnav.py:760`, `consensus_eps.py:205`; re-pin (mandatory) `test_report.py:31-47,49,68`, `test_carveout.py:526,539,545,553,560`, `test_marks.py:87`, `test_transactions.py:210`, `test_consensus_eps.py:72`, `test_justified_pnav.py:291-296`; `git rm --cached outputs/*.xlsx` + `.gitignore`; README:355-366, METHODOLOGY §7.3/7.4 | Nothing a reader has used (md carries the same sections) |
| 39 | `transaction_anchor_comparison.md` (every regen; largest stage, 8 s) | outputs | **Partly rescued**: governance `wo3_phases0-3_review_2026-07-09.md:86,114` filed a finding because the committed md was stale; `.claude/commands/news-pull.md:81` and CLAUDE.md:121-122 instruct reading it after a promotion; only rendered per-class print register. Not cited by the 8/31 triage or 9/01 round — the drift gate was the detector | 7/09 (decisive) | 27 KB md + xlsx per regen | **ON-DEMAND, run with the S&P round** | Remove from `main()` (`pipeline.py:1043`); add `--txn-comparison`; the promotion procedure runs it and commits the md WITH the round; do not gitignore; edit CLAUDE.md:121-122 + news-pull.md:81 ("drift gate + `pipeline --txn-comparison`"); tests call the function directly (unaffected) | Nothing (the round artifact survives) |
| 40 | `consensus_eps_xref` stage (`pipeline.py:1026-1027`) + module + 8 tests | outputs/guards | NONE — 0 decisions since June (`gnk_log:3838`, 6/11), 0 scorecard fields (`grep -ci eps book_scorecard.*` = 0), 0 governance, no importer but pipeline | never | 56 pure-regen commits | **PRUNE from `main()`; module ON-DEMAND or delete** | Delete the call; `git rm outputs/consensus_eps_xref.*`; if deleting the module, README "639+" need NOT change (639 ≤ 1.25×631); mark §9.11 + LIMITATIONS:150 retired | A cross-check the cycle weighting already prices |
| 41 | `dividend_window_test.md` per-run render | outputs | `overlay_ledger.py:110` (ledger last written 6/22); CHANGELOG:1354 E-2 made §12.6 "a control, not docs" via `dividend_window_rows`; NAT relabel is static (`provenance.py:145`) | 6/22 | 6 s + md | **ON-DEMAND** | Drop `pipeline.py:1040-1041`; keep `dividend_window.py` + `build_rows` + `test_dividend_window` (the §12.6 control) | — |
| 42 | `decisions/<t>_log.md` auto-prepend ×25 per run | outputs/guards | `drift_gate.decision_log_annotated_since` reads ONLY the top entry of a breaching name; governance `reviews/2026-07-02-cmbt-minireview-DRAFT.md:23-31` rebuilt a waterfall from auto entries once (incl. a +$0.05 step) | protocol live | 5,042 auto vs 163 human headers; 2.9 MB; sb_log 178/186 auto; "expected churn" (PLAN:822) | **SIMPLIFY — fork F12** | Prepend only for `delta.material` or gate-breaching names; gate in `pipeline._run_delta_and_decision_log` (NOT `delta.py` — `drift_gate` imports `delta`, circular); keep the 7/28 anchor rule (CHANGELOG:283-288); point `decisions/README.md:28-31` at `git log -p outputs/book_scorecard.json` for waterfalls; re-point `test_delta.py:221-288` ×4 (behaviour tests); `test_drift_gate.py:175-188` unchanged; PLAN:824 + `commit_drift.sh:11` comments | Sub-threshold per-run steps in the logs (recoverable from scorecard history) |
| 43 | `outputs/refresh_checklist.md` (tracked, in `drift_files.txt`) | outputs | the MODULE is live (sentinel, edgar_poll import it); the FILE: 0 citations, 0 tests, no cron writes it (over-enumerated since 7/06) | 8/31 | 28 KB; 3 commits | **DOWNGRADE** | Untrack via `.gitignore` (not only `--cached`, or a hand-run `refresh` trips the price cron's stand-down); drop from `drift_files.txt`; keep the command as report-day preflight | — |
| 44 | `ffa_vs_strip.{py,md}`; `terminal_value_sensitivity.py` + outputs | outputs/guards | historical only (`gnk_log:3842`, METHODOLOGY:1836-1837; memo consumed 6/22) | 6/11; 6/22 | none | **PRUNE / ARCHIVE** | `git rm scripts/ffa_vs_strip.py outputs/ffa_vs_strip.md`; move `terminal_value_*` with the archive (row 48) | — |
| 45 | `pareto_mentions_<t>.md` ×20 | outputs | 6/10 retro-sweep entries only; WORKFLOWS:93 already on-demand | 6/10 | 250 KB | **PRUNE** (regenerate on demand) | `git rm`; fix `nat_reconciliation_prereg_2026-06-30.md:17` pointer | — |
| 46 | `outputs/weight_robustness.yaml` (the §9.10 field) | outputs/registers/guards | `weight_sign_stable`/`ev_pct_family_min/max` → governance `TRADE_PREREG.md:52-56` rule 1 (rewritten 8/31 with the CCEC example), `orders/2026-07-15-ccec-tranche1.md`, `holdings/ccec.md` 8/11, `stage_a_halt_investigation_2026-08-10.md:41`, `tier_semantics_amendment_2026-08-13.md:18,77`; edge-cleared long definition `scorecard.py:581-605` | 8/31 | — | **KEEP** | — | — |
| 47 | Five family sidecar scripts + sha/containment machinery | outputs/registers | as row 46 | 7/15 (TEN containment catch — an artifact of the sidecar's own vintage) | rewritten on 19 dates since 7/01 (43 commits); raw-bytes sha (`scorecard.py:401-409`) reds on a comment edit; EXACT containment (`:516-525`) forces re-run on any EV move → regen-sidecars-regen double run; "five sidecars re-run" in every promotion doc | **SIMPLIFY** | `run_weight_family_sidecar(quarter)` stage before `run_scorecard_xref` (CHANGELOG:396-397 7/15 already proposed it); bracket sets stay OUT of `scenario_inputs.yaml` (import from scripts; CHANGELOG:706-714); runs from the production entry (8/14 doctrine); sha of parsed weights; five per-script refactors (lng/product differ in shape); re-pin `test_sentinel.py:107`, `test_scorecard.py:474`; `SIDECAR-STALE`/`ev_lagging`/`test_outputs_hygiene:45-55` become tautologies → delete; keep `test_sidecar_merge_preserves_all_other_families` | The WO1-F4 "outside determinant scope" separation (a regen-ordering fix the fold removes at root) |
| 48 | Family by-products (`weight_robustness_diagnostic.{md,xlsx}`, `dry_bulk_weight_robustness.{md,xlsx}`, `lng_weight_robustness.md`, `lpg_*.{md,xlsx}`, `product_weight_comparison.md`) | outputs | `dry_bulk_weight_robustness.md` cited once by governance `funnels/drybulk_2026H2/2343.md:20,123` (7/13); the other 7: NONE | 7/13 | 8 files/regen | **PRUNE writers; dry-bulk md ON-DEMAND** | stop the md/xlsx writers; leave a pointer in §9.10; README:80-82 | per-set tables (producible on demand) |
| 49 | Archival docs parked in `outputs/` (audits, memos, `cmbt_onboarding/`, VIE xrefs ×3, `mb_*` June ×3, `overlay_ledger.md`) | outputs | history cites only; **`ccec_buy_diagnostic.md` is auto-linked into `ccec_fv_report.md` (`report.py:297-326`) — stays**; VIE xrefs are `analyst_target` provenance in `watchlist.yaml:91,114,133`; `cmbt_onboarding` is figure provenance in `cmbt.yaml:6`, `cmbt_2026-Q1.yaml:5,64`, `dividend_policies/cmbt.yaml:8`; `terminal_value_options_memo` cited by `dividend_strip.py:51` | Jun-Jul | confusion + grep noise | **ARCHIVE** (`docs/archive/`) | `git mv` + sed EVERY citation incl. input files and code (`test_manifest_provenance` does not check path existence, so dangling cites red nothing); fix README:80-82 ("5 standalone diagnostics" is false — no script produces the VIE xrefs) and README:105-120 | Cold-reader discoverability (one README line) |
| 50 | `inputs/overlays.yaml` + `overlay_ledger.py` + test + `outputs/overlay_ledger.md` (§16) | registers/guards | NONE — not read by pipeline/scorecard/governance (grep 0); ledger Q1-stamped, rows keyed to "Jun-9 weights" (3 revisions stale); guards auditor kept it as "cheap" | 6/22 (E-2 closure, about the ledger itself) | 81+170+72 lines | **PRUNE — fork F4** | `git rm` the four; METHODOLOGY §16 → a 6-row table with the same retire conditions; §15/§12.6 rows are machine-derived elsewhere (row 41 keeps the §12.6 control); no README census guard to adjust (refuter: that step is a no-op) | A rendered file nobody re-rendered since June |
| 51 | `backtest/` (Test 0/1/2) | outputs/guards | NONE in src (comments only); `pyproject.toml testpaths=["tests"]` so CI never ran it; `REPORT.md` "no demonstrated edge"; Test 2 "+8q (~end-2028)" | 7/14 | week-close `pytest backtest/` (PLAN:817) | **ARCHIVE** (keep tracked, drop from the gate) | delete PLAN:817 line; keep `DATA_CONTRACT_NORMAL_RATES.md` (cited by src); note `PRE_REGISTRATION_TEST2.md:52-54` orders a per-quarter re-run nobody owns → fork F15 | Nothing until 2028 |
| 52 | `.claude/worktrees/` ×3 (35 MB) | outputs/guards | NONE; they triple every grep hit | — | grep noise | **PRUNE — verify first (fork F16)** | Guards refuter: `3044eb3 ≡ 6f6648a` by cherry-pick, `ebb160f ≡ fc95e8d`; outputs refuter flagged `affectionate-goldstine` as unmerged — UNVERIFIED by me; `git cherry main claude/affectionate-goldstine-e79035` then `git worktree remove` ×3 + `prune` | — |
| 53 | Trigger-register mechanism (`reweight_triggers.yaml` → `refresh.check_reweight_triggers` → `TRIGGER-DUE`) | registers | the only thing that can page a dated decision; did so 7/12 (Doha) | 7/12 | 30 commits/17 sittings; 539 lines | **KEEP mechanism; PRUNE file to ~10 cards** | rows 54-62; NOTE the scheduled news-pull SKILL.md does NOT read the register (only the manual `/news-pull` does) — the "6k tokens/week" saving is fiction; the cost is git noise + human reading | — |
| 54 | 8 `done` cards (~190 lines) | registers | `test_refresh.py:280-281` pins two by name; `scenario_inputs.yaml:218,289,294,816` comments cite `crude_day60_toll_cliff` | — | re-read per sitting | **ARCHIVE** → `decisions/reweight_trigger_archive.md` | re-pin the test; repoint the four comments | — |
| 55 | `crude_transit_normalization`, `crude_brent_reopening` (due=null) | registers | NONE — 0 decisions, 0 digests (0/6 `news_digest_*` mention war-risk or Brent), cannot page | never | prose | **RETIRE — fork F8** | delete `:274-298`; if the owner still wants the war-risk level, put ONE line in the scheduled task's SKILL.md (the runner), not `news-pull.md` | An observable nobody observed for 9 weeks |
| 56 | `hormuz_fee_collection_watch` + `escalation_pause_corroboration` (weekly) | registers | Scenario weights → PW-FV for 10 crude names → scorecard; registered by owner rulings R3/R5 (`crude_day60_toll_cliff_2026-08-16`), re-cadenced 8/25 "owner may re-cadence"; `scenario_inputs.yaml:225` names escalation_pause the successor to the declined C3 tilt | 8/25 | 10 sittings, ~1,340 lines of docs since 7/01; B′ (7/31) and C2 (8/16) moved ZERO labels, ≤2.5pp EV; only 7/12 flipped labels | **SIMPLIFY to one monthly card — owner fork F3** | after WO5 (9/04-05) re-expresses mou_bear the action text is stale anyway; fold per the registers audit §3.3 YAML | weekly cadence on the pause (a PAGE-worthy strike shows in the daily news layer regardless) |
| 57 | `tanker_forward_print_lands` (Stage B window 8/26→9/04; ±10% gate prose-only) | registers | `twelve_month_tc.yaml` tanker rows → 14 strips; Stage A 8/10 RATIFY `d510311`; Rider 4 promotions 7/13, 7/24, 8/09, 8/24, 8/31 | 8/31 | 408 lines of stage docs | **KEEP until 9/04, then RETIRE with row 30** | Rider 4 → WORKFLOWS standing rule ("a tanker FFA/12M print promotes on sight with a frozen predicted-impact block") — this IS the automation lane the owner asked for; keep `UNINGESTED-PRINTS twelve_month_tc` nag | — |
| 58 | `all_sectors_consensus_pair_recapture` (10/02) | registers | only 7/06 rebase was card-driven; 8/13, 8/31 were static-guard/42d-lane driven | 7/06 | 1 card | **RETIRE** | delete; edit WORKFLOWS:282, `test_approx_roster.py:53` comment; close its own "14d vs quarterly" question with a dated line (= the 42d lane) | — |
| 59 | `all_sectors_quarterly_staleness_floor` (10/02) | registers | NONE — never fired, 0 decisions, no owner ruling ordered it | never | 1 card | **RETIRE** (fold into row 60) | — | — |
| 60 | `product_glut_arrival_timing` (10/02) | registers | product Set B v2 weights; key cited `scenario_inputs.yaml:816-817,921`, METHODOLOGY:1089, PLAN:784 | 8/16 | 1 card | **KEEP as the merged 10/02 card — keep the KEY** (not `quarterly_review_2026Q4`) | absorb the staleness-floor line (b) | — |
| 61 | `vlgc_realized_tce_refresh` (due=null) | registers | **failed**: BWLP Q2 refreshed 8/31, `twelve_month_tc.yaml:50,70` still VLCG 2026-06-02/63,615; no check recorded | never | prose | **RETIRE** → dated line on `lpg_v1_lock_rerun` | fix `test_lpg_sector.py:212` comment; governance `wo3_phases0-3_review_2026-07-09.md:93,115,132` cited the card as MET | — |
| 62 | `lpg_anchor_annual_review` (fold into tce_means) | registers | **RESCUED**: separation is deliberate and test-locked (`test_refresh.py:454`), 7 pointers incl. governance; a 2027-06-30 card costs nothing | — | 20 lines | **KEEP both** | — | — |
| 63 | `handy_bulk_txn_refit`, `ppmx_txn_refit` (due=null) | registers | **RESCUED as identifiers**: `provenance.py:99,190,218` tier sub-reason strings (no-drift-tested registry), `basis_status.yaml:40`, governance `holdings/sb.md:48`; PPMX condition NOT met (1 of 2 young) — no refit owed | — | nil | **KEEP keys** | — | — |
| 64 | `container_mb_refresh`, `lpg_v1_lock_rerun`, `tce_means_semiannual_review`, `check_watchlist_freshness` + APPROX roster, `earnings_calendar.yaml` + sweep, `agent_duties.yaml`, `archive_gaps.yaml` (after fix), R-2/F-1 ratio restatement | registers | 7 container decisions + MPCC/GSL NAV; LPG tier; `historical_tce_means` → cycle; `pnav_basis` → governance; `edgar_poll.py:78-87` cadence + 8/18 FLNG catch; 8/24 dead-task catch; 8/16 gap; governance charter | 8/31 | — | **KEEP** | earnings flags simplify per rows 24-26 | — |
| 65 | G6 freeze (prose in 8 decision files) | registers | fired 8/09 (`dcab848` RATIFY) and 8/31 | 8/31 | prose restated per round | **SIMPLIFY — codify** | in `drift_gate.py:285-320`: flip ∧ ΔNAV=0 ∧ Δprice=0 ⇒ rates-leg-only ⇒ `FROZEN-FOR-OWNER-EYEBALL` (there is no cause string — the auditor's mechanism was wrong) | — |
| 66 | spot_tce hold + `UNINGESTED-PRINTS spot_tce` nag | registers | `spot_tce` feeds `breakeven.py`/`report.py:265`/`validate.py:32` only — diagnostic, not NAV/strip | never a valuation event | 14 daily lines + an "annotate the hold" chore | **DOWNGRADE — fork F17** | drop from the lane (`sentinel.py:272`; re-pin `test_sentinel.py:281-286`); "auto-promote" needs new code (no spot parser: `test_sp_scan.py:80`) | — |
| 67 | `silence_days: 14` summer override | registers | `sentinel.py:311-314`; daily resumed 8/07 | — | lane muted 11 extra days | **PRUNE now** | drop the key + comment at `rocketchat_sources.yaml:25` | — |
| 68 | SBLK flip-margin literal (`test_tier_semantics_amendment.py:409`, −8.48) | guards/registers | test only; re-pinned 8/24, 8/28, 9/01; never caught anything | never | 1 re-pin per SBLK-moving regen | **SIMPLIFY** | recompute-from-row assert (`flip_margin_pct(row.nav, row.j_par, row.j_hist, row.price)`, abs 0.005) + `> READ_FLAG_HYST_PCT` | — |
| 69 | Absolute FV/NAV/count pins (`test_scenarios` 9 bands, `test_report`, `test_cycle_blend`, `test_fro/eco/capt/brut/mpcc_gsl/bwlp/lpg/sb/sblk/gnk/cmdb/cmbt`, `test_justified_pnav` 8.52, `test_dividend_strip` DHT) | guards | regression guard; **no absolute band has a recorded catch** — every commit since 7/20 is a re-pin (`fd87bbc`, `a8e9914`, …); real catches came from identity guards and prereg BANDS (SB 7/31, CAPT 9/01); `63b48b3` was a wrongly-GREEN pin | never | 7 re-pin commits / 6 wks; `test_scenarios` 14 commits | **SIMPLIFY** | ONE scorecard-agreement test (`value_company` reproduces the committed `book_scorecard.json` row); prereg band where one exists; LOCK tests survive (row 71); `test_confidence_tier::test_archetypes_from_scorecard` stays as a ruling lock; caveat: price-vintage seam reproduction UNVERIFIED without a run | judgment moves to one place (gate + frozen band) |
| 70 | YAML-echo pins (bwlp debt/leases/shares, lpg, capt 1,806.6M, sblk 122,035,000, fleet counts) | guards | pure echoes — EXCEPT ruling-shaped zeros: `test_bwlp.py:48-49` NB commitments/advances == 0 (held out pending `bwlp_nb_order_fork_2026-08-31`), `test_capt.py:38-40` advances/governance == 0 (declined, capt_log) | never (echoes) | re-pins per refresh | **SIMPLIFY** | drop caption echoes; KEEP ruling-shaped asserts; keep `test_bwlp_nci`, `test_handy_bulk_class` (the guards that fired 8/31) | — |
| 71 | Structural guards + LOCK tests (`test_manifest_provenance`, `test_scrubber_provenance`, `test_newbuild_convention`, `test_quarter_coherence`, `test_thread1_resale_anchor`, `test_handy_bulk_class` statics, `test_bwlp_nci`, `test_market_data_vintages`, `test_outputs_hygiene` F-13, `test_tier_is_price_invariant`, `test_drift_gate`, `test_scorecard`, `test_carveout`, locked-weight tests, `test_approx_roster`, `test_earnings_calendar`, pollers' known-miss fixtures, `test_sentinel`/`test_notify`, `test_plists`, `test_cron_guard`…) | guards | catches on record: 8/08 pair guard, 8/31 handy ×0.90 + as_of default + NCI (`011c733`, `a8e9914`), FLNG 8/25 / TRMD 8/29 `-dirty`, MPCC roster 8/09 | 8/31 | zero when green | **KEEP** | — | — |
| 72 | Ledger duplication (`OPERATING_SCRUBBER_*`, `NAV_FIGURE_ESTIMATE_QUEUE`, `OFF_CONVENTION_QUEUE` in tests AND `provenance.py`) + `test_provenance_queues_track_the_guards` | guards | `scorecard.py:58,244` reads the src copy → `op_err` → tier; the test polices a duplication ("CAPT 5→8 BOTH surfaces", `aa7556f`) | — | double edits | **SIMPLIFY** | single-source in `provenance.py`; tests import; delete the agreement test (xfail-strict still surfaces a queue edit) | — |
| 73 | `test_handy_bulk_routed_only_by_2343`; `test_normal_rates` `FROZEN_BANDS = PARITY_BANDS` self-reference; `test_docs_stay_lean` README floor; `test_lpg_sector` live numerator inside the F-2 synthetic e2e; 24 files with hard-coded quarter literals (`test_cmbt` ×6 first — CMBT Q2 lands 9/03) | guards | tripwire "until PANL" (covered at :68-79); bands equal PRE_REG A1.2 today; README 639+ vs census 639 — next test added reds README; F-2 keeps the e2e; `conftest.BOOK_QUARTER` exists | — | forced edits | **PRUNE / fix / fix / SIMPLIFY / SIMPLIFY** | delete the tripwire; copy the 9 bands as literals; flip to `claimed <= defs <= 1.25×claimed`; split the numerator pin; finish the BOOK_QUARTER sweep | — |
| 74 | PLAN.md (824 lines, ~20k tokens, read every session) | guards | agent handoff; `scripts/ratify_baseline.sh:37` greps the literal `PENDING OWNER: baseline ratify` | every session | 30 rewrite commits; 684 lines of July history | **SIMPLIFY** | cut to ≤150 lines (state / OWNER OWES / NEXT / verification gate); `git mv` the rest to `decisions/plan_archive_2026-07.md`; keep the ratify marker | ~17k tokens/session |
| 75 | CHANGELOG vs METHODOLOGY Appendix A dual week-close entries | guards | no test reads Appendix A; `marks.py:37` cites one 6/12 item; WORKFLOWS:325 orders the weekly entry | — | double writing | **SIMPLIFY** | Appendix A framework-only; strike WORKFLOWS:325 | — |
| 76 | `PRE_REGISTRATION_*.md` ×8 at root | guards | docstrings/comments only (`normal_rates.py`, `justified_pnav.py:232,607`, `loaders.py:307`, 4 tests); nothing opens them; THREAD1A 0 refs | ≤7/15 | root clutter | **ARCHIVE** → `decisions/preregs/` | `git mv`; fix path strings in the docstrings; README §Documentation line | — |
| 77 | WO3 / WO4 at root | guards | cited as Authority by `lpg_log:2344`, `bwlp_log:2372`, `lpg_methodology_2026-07-07:3`, `vlgc_marks_2026-07-09:3`, `lpg_weight_comparison.py:85`; governance `sector_charter_2026H2.md:154`, `drybulk_2026H2_rubric.md:171` by relative path | 7/08 | — | **ARCHIVE** → `decisions/work_orders/` | update the two live governance funnel docs (or leave one-line root stubs) | — |
| 78 | WO5 | guards | executes R4 9/04-05 | 9/01 | — | **KEEP until executed**, then archive | — | — |
| 79 | TICKER_NOTES.md | guards | no machine reader; week-close reconcile step (WORKFLOWS:326); CLAUDE.md:11 pointer; last substantive touch 7/18 | 7/18 | a week-close step | **ARCHIVE after merge — fork F13** | fold each quick-ref into the `<t>_log.md` header; drop WORKFLOWS:326 + the CLAUDE.md bullet (frees chars against the cap) | a hand quick-ref (content survives) |
| 80 | LIMITATIONS.md; PERMISSIONS_PROPOSAL.md | guards | README "credibility doc" ×3; CLAUDE.md:14,161 pointers; the IBKR §6 Q4 denial is cited nowhere in governance | — | weekly audit item; 0 commits since 7/20 | **DOWNGRADE to quarter-close; ARCHIVE** | add a "Closed" appendix; `git mv` to `decisions/permissions_allowlist_2026-06-12.md`, keep one CLAUDE.md pointer | — |
| 81 | Week-close documentation audit (WORKFLOWS:319-335 step 1) | guards | owner decision 6/11; performed in full twice since 7/20 (7/26, 8/16), skipped 8/23, 8/30; one material catch (7/18 spot re-proxy never wired, CHANGELOG:412-414 — a promotion-execution miss guardable by a staged-vs-live agreement test) | 7/18 | 1-2 agent-hours + 30-60 owner-min per full run; 11% of commits | **SIMPLIFY — owner fork F5** | docs updated in the deciding commit (already the practice); PLAN edited at sitting close; keep gate / clean git / push; drop TICKER_NOTES, LIMITATIONS, Appendix A, backtest/harvester lines from PLAN:810-824 | stale-counter catches (already guarded) |
| 82 | README cheatsheet (`src/ 16 modules`, `scripts/ ← PDF build script`; "Six launchd jobs"; staging table) | guards | cold readers; `test_docs_stay_lean` pins 3 counters | — | — | **KEEP; fix once** | reality 45 modules / 28 scripts; update per every row above | — |

## 3. Grouped removal plan

**(a) Stop now — zero risk, no consumer, no ruling in the way:** rows 5, 13, 17, 30 (lane half; card half waits for 9/04), 31, 38, 40, 44, 45, 67, 73 (tripwire + floor flip + bands literal), plus the four "fix first" items in §0. Also row 16's plists (owner `launchctl`, the only step the agent cannot do) and row 4's lane (freeze the CSV, delete the parser). One commit per removal with a dated CHANGELOG line; README:324 "Six launchd jobs" stays six (the ctxprobe plists were never in the inventory — `test_plists` unaffected).

**(b) Downgrade — cadence or log-only:** rows 6, 7, 20-28, 33-34, 36, 43, 48, 51, 66. The sentinel rewrite is one PR: `notify.yaml` gains a `weekly` route; `sentinel.py` edits at 101-104, 266-290, 446-456, 465-474, 674-686, 706-744, 880-883, 894-909; persisted "previous digest" hash in `sentinel_state.json` (the 7/28 spec's mechanism); tests to re-pin: `test_filing_events_landed_overdue_unseeded`, `test_earnings_unconfirmed_pages_near_expected_windows`, `test_fetch_failed_promotes_to_page_in_open_window`, `test_digest_streak_escalates_once`, `test_exit_codes_and_log_format`, `test_notify::test_real_routing_table_covers_every_live_sentinel_tag`; docs README:302-320, WORKFLOWS:251-273; mark `digest_noise_proposal_2026-07-28.md` decided. Expected pages: TRIGGER-DUE first day, report-class FILING-LANDED once, FILING-OVERDUE, SURFACE-INCOHERENT, DIRTY-TOO-LONG, EARNINGS-UNCONFIRMED first sighting — 2-6/month.

**(c) Convert — brittle value pins → invariants/bands:** rows 42, 47, 65, 68-70, 72, 73 (numerator split, BOOK_QUARTER sweep). Order: (1) fold the family sidecar into the pipeline (removes the double regen and the "five sidecars" step); (2) scorecard-agreement test replacing the absolute bands; (3) breach-only log prepend; (4) G6 codified; (5) the small fixes. After (1)-(3) a promotion round re-pins nothing and writes ≤5 log entries instead of 25.

**(d) Archive — docs:** rows 49, 54, 74-77, 79, 80. All `git mv` + sed; the trap is INPUT-file citations (watchlist, cmbt manifests/sheets/dividend policy, `dividend_strip.py:51`) that no test validates — sed them in the same commit.

**(e) Contested — auditor vs refuter, owner forks:**
- **Harvester.** Auditor (outputs/guards): prune cron + flags + CI; refuters (fetch/outputs/guards): rescued — it parses the xclusiv Resale line that IS the age-0 NAV anchor, and supplies ~35% of promoted print sources. Recommendation: KEEP the crawl (row 8), PRUNE the CI job with an N-5 re-disposition line (row 9), re-point the marks-trail flag at MB.
- **Crude geopolitics cards.** Auditor: fold to one monthly card now; refuter: R3/R5 owner rulings + 8/25 re-cadence make this an owner word. Recommendation: fold, AFTER WO5 lands 9/04-05 (the action text goes stale then anyway).
- **Overlay ledger.** Registers auditor + refuter: prune; guards auditor: keep as cheap; outputs refuter: E-2 (6/22) made §12.6 a control via this path. Recommendation: prune the overlay files (row 50), keep `dividend_window.py` + its test as the §12.6 control (row 41) — E-2's substance survives.
- **`EARNINGS-SWEEP-STALE` cadence.** Auditor weekly page; refuter daily-digest + page once at >7d (owner directive 7/21 bounds it). Recommendation: refuter's form.
- **Digest-streak escalation.** Auditor keep (count non-META); refuter prune. Recommendation: prune, keep `FETCH-FAILED: 2` as a threshold.
- **`sentinel-lite` sentinel job cadence.** Auditor weekly; refuter daily (10 s, only off-machine TRIGGER-DUE path). Recommendation: daily, comment-on-change.
- **ctxprobe scripts/log.** Fetch refuter: keep until `close_acceptance` runs or is written off; sentinel/guards: prune with `close_acceptance`. Recommendation: write off WO2 acceptance (fork F6) and prune all.
- **`ffa_drybulk/2017..2024`.** Both agree no reader; the deletion is irreversible and RC retention is UNVERIFIED. Recommendation: leave on disk (zero recurring cost).
- **Worktree `affectionate-goldstine`.** Outputs refuter: 1 unmerged commit; guards refuter: cherry-picked as `6f6648a`. UNVERIFIED — `git cherry` before removal.
- **Baltic CSV.** Fetch auditor `git rm` or freeze; guards refuter: freeze (only in-repo Baltic history). Recommendation: freeze.

## 4. What the pruning does to the autopilot

Per the scout brief, an S&P promotion round today is: triage doc → owner go → promotion doc → commit inputs → regen → 25 log annotations → five sidecars → test re-pins → commit → ratify → push, with a regen-sidecars-regen double run (9/01 shows it twice: 13:44+13:46, 17:53+17:55 UTC). After (c): **sidecar step gone** (in-process stage; 19 re-run dates since 7/01 → 0; SIDECAR-STALE/`ev_lagging` unreachable), **pin step gone** (7 re-pin commits in 6 weeks → 0; `test_scenarios` 14 commits → ≤2 for genuine reweights), **double regen gone** (one ~50 s run), **25 log writes → only breaching names** (typically 0-5), **`transaction_anchor_comparison` produced once per round as the audit artifact** instead of per regen, and the round's predicted-impact block plus the drift gate remain the only judgment surface — which is exactly where the 7/31 SB and 9/01 CAPT catches came from. The FFA lane after row 57: Rider 4 becomes a standing rule with a frozen predicted-impact block and G6 auto-classified (row 65) — the two documented traps (OCR transposition, missed cycle channel) are addressed by the month-end grid fix and by the gate, not by owner eyeballing. Flags to route: 19 live tags → ~13 (TRIGGER-EVIDENCE, DISK-PRESSURE gone; FLEET-TRANSACTION merged; check_market_data lane gone; two EARNINGS tags routed explicitly instead of via the unknown-tag rule). Digest: ~33 lines/day → one Saturday email of ~10 lines plus a one-line daily alive mail; pages ~20/month → 2-6. Register read per sitting: 539 → ~150 lines. Session context: PLAN ~20k → ~4k tokens; CLAUDE.md gains ~200 chars of headroom (TICKER_NOTES + PERMISSIONS lines). Owner-minutes/week saved, order of magnitude: digest/page triage ~30-45; sidecar/pin/double-regen ritual ~20-30 in season; week-close doc audit ~15 (amortised); red-CI inbox ~10. Call it 1-1.5 owner-hours/week, none of it valuation judgment.

## 5. Risks and reversibility

Everything tracked is git-reversible by construction; each removal is one commit with a CHANGELOG line, so `git revert` restores it. **Not reversible:** deletions of gitignored disk — `inputs/ffa_drybulk/2017..2024`, `inputs/baltic_capesize/`, `shipping_harvester/data/`, `state/fdprobe`, worktrees. Of these only `fdprobe`/`capt_*.txt` and the CPLazos PNGs are recommended; RC re-pull of either archive is UNVERIFIED (no network in the audit).

**Removals that need an owner word because a dated ruling created the piece:** harvester CI job (N-5, 7/14 external audit); `EARNINGS-UNCONFIRMED`/`SWEEP-STALE` cadence (7/21 directive `3fa799e`); hormuz_fee/escalation_pause cards (R3/R5 8/16; re-cadence 8/25); week-close checklist (owner decision 6/11); overlay ledger (E-2 closure 6/22); `test_lpg_sector` synthetic e2e (F-2 7/09 — split, don't delete); `test_archetypes_from_scorecard` (8/13 amendment — keep as lock); the WO1-F4 sidecar separation (removed at root by the fold); 2.4b design ("DO NOT ENABLE before owner sign-off" — deleting it is the conservative direction); the ctxprobe/WO2 acceptance write-off; `launchctl` and `git push` are owner-only by settings.

**Pre-conditions before anything else lands:** commit the `archive_gaps.yaml` fix alone (the tree is dirty on `prices_daily.yaml` too — `feedback_isolate_commit_from_price_drift`); fix the two F401s so CI can actually run the suite on the pruned tree; the 9/02 sentinel will be META-MODE until the tree is clean — do not read its silence as health.

**Lines to edit (router + runbook + README):** CLAUDE.md:11 (TICKER_NOTES bullet), :14 and :161 (PERMISSIONS_PROPOSAL pointers → archived path), :121-122 ("that IS the drift gate — read `transaction_anchor_comparison.md`" → "drift gate + `pipeline --txn-comparison` with the round"), the "Other commands" line (drop `ffa_vs_strip`/consensus_eps if referenced), the Week-close bullet (3-step form). WORKFLOWS.md:93 (mentions on-demand, already), :181-182 and :219-222 (links chain), :195-199 (venv note stays if harvester stays), :232 (draft queue), :251-273 (sentinel tags), :282 (consensus recapture trigger), :319-335 (week-close), :325 (Appendix A), new §Data-sources standing rule for Rider 4. README:76 (test count, only if a test FILE is deleted and the floor is flipped), :80-82, :105-120, :294, :302-320, :306-310 (dead-man claim), :324, :333-334, :343-352 (staging table rows for baltic/capesize/links), :355-366 (xlsx), cheatsheet. PLAN: items 2-3 (WO2 residue), :148, :257-258, :282, :805-824 (verification gate lines), then the cut. `decisions/launchagents_reconciliation_2026-07-03.md` inventory block only if a plist in `scripts/` changes (none recommended). `scripts/drift_files.txt` loses three lines (baltic csv, `refresh_checklist.md`, `pareto_daily_links.json`) — three readers, re-pin `test_cron_guard.py:83`.

**Residual risks:** (1) the scorecard-agreement test must reproduce rows across the price-vintage seam — UNVERIFIED without a run; keep the LOCK tests and the ruling-shaped zeros regardless. (2) FILING-LANDED report-class gating from `primary_doc` names will miss a 6-K whose primary doc is a cover page with the report as an exhibit — the weekly line still carries it; the open-window `STALE-BALANCE-SHEET` line is the backstop. (3) Weekly-only `UNINGESTED-PRINTS` delays a marks triage by ≤6 days — acceptable since rounds run fortnightly. (4) Breach-only log prepend loses the sub-threshold per-run steps a governance mini-review once rebuilt from `cmbt_log` — documented alternative is `git log -p outputs/book_scorecard.json`.

## 6. OPEN FORKS (one word each)

| # | Question | Rec |
|---|---|---|
| F1 | Remove the harvester CI job (N-5 re-disposition logged)? | **yes** |
| F2 | Keep the harvester Saturday crawl (xclusiv anchor + 35% of print sources)? | **keep** |
| F3 | Fold hormuz_fee + escalation_pause into one monthly card after WO5 lands? | **yes** |
| F4 | Prune overlays.yaml/overlay_ledger (dividend_window stays as the §12.6 control)? | **prune** |
| F5 | Replace the weekly doc audit with same-commit doc updates + a ≤150-line PLAN? | **yes** |
| F6 | Write off WO2 acceptance (delete close_acceptance, receipts, drill doc, ctxprobe scripts/log; you run the two `launchctl unload`s)? | **yes** |
| F7 | Delete `inputs/ffa_drybulk/2017..2024` (1.5 GB, irreversible)? | **no** |
| F8 | Drop transit_normalization/brent_reopening outright, or one line in the scheduled news-pull SKILL.md? | **one line** |
| F9 | `sentinel-lite` sentinel job daily (comment-on-change) or weekly? | **daily** |
| F10 | Prune digest-streak escalation (keep FETCH-FAILED:2 as a threshold)? | **prune** |
| F11 | EARNINGS-SWEEP-STALE: daily digest + page once at >7d in season, or Saturday-only page? | **digest+once** |
| F12 | Decision-log auto entry only for material/breaching names? | **yes** |
| F13 | Merge TICKER_NOTES into the log headers and delete it? | **yes** |
| F14 | Baltic CSV: freeze in git or delete? | **freeze** |
| F15 | The PRE_REGISTRATION_TEST2 per-quarter re-run duty: assign or drop until 2028? | **drop** |
| F16 | Remove all three worktrees after `git cherry` confirms the goldstine commit is in main? | **yes** |
| F17 | Drop spot_tce from the UNINGESTED lane (no auto-promote — no parser exists)? | **yes** |
| F18 | `rm -rf shipping_harvester/data` (195 MB; two decision citations point into it)? | **no** |
| F19 | Retire `tanker_forward_print_lands` + TRIGGER-EVIDENCE lane the day Stage B closes (9/04), Rider 4 → WORKFLOWS standing rule? | **yes** |
| F20 | Retire `all_sectors_consensus_pair_recapture`, `quarterly_staleness_floor`, `vlgc_realized_tce_refresh`; archive the 8 done cards (register 23 → 10)? | **yes** |